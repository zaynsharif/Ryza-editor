#include "GUI/MainWindow.h"
#include <iostream>

int main(int argc, char* argv[]) {
    // Initialize the application
    std::cout << "Starting Code Editor..." << std::endl;

    // Create main window instance (replace with actual GUI framework logic)
    MainWindow mainWindow;
    mainWindow.show();

    return 0;
}
